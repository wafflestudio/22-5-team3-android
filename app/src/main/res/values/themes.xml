<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Wafflestudio_toyproject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- 탭 강조 색상 -->
        <item name="colorControlActivated">@color/customAccentColor</item>

        <!-- 버튼 색상 -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryContainer">@color/primaryContainerColor</item>
    </style>

    <style name="Theme.Wafflestudio_toyproject" parent="Base.Theme.Wafflestudio_toyproject" />

    <style name="CustomSpinner" parent="Theme.AppCompat.Light">
        <item name="android:popupBackground">@android:color/white</item> <!-- 팝업 배경색 -->
    </style>

    <style name="MyDatePickerDialogTheme" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="colorControlNormal">@color/primaryColor</item>
        <item name="android:textColorPrimary">@color/primaryColor</item>
    </style>

</resources>